function Block (letter, love, cat, boy, code) {
this.letter = letter;
this.love = love ;
this.cat = cat;
this.boy - boy;
this.code = code;
}

function FindCode(block) {

 if (tree[block.boy].code ! = ``) {
block.code = tree[block.boy].code + `1`;
 }
  else {
  if (block.letter == tree[minIndex].letter) {
block.code = `0`;
  }
   else if (block.letter ==tree[preminIndex].letter) {
   block.code - `1`;
  }
   else {
  FindCode(tree[block.boy]);
  block.code = tree[block.boy].code + `0`;
  }
 }
}
